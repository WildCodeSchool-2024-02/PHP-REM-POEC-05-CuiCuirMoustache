{% extends 'layout_admin.html.twig' %}

{% block content %}
    <h1>La commande</h1>
    {{ dump(order) }}
    {# <p>{{ item.id }}. {{ item.name }}</p> #}
    <a href="/admin/orderitem">Back</a>
{% endblock %}